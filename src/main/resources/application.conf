akka {
  loggers = [akka.event.slf4j.Slf4jLogger]
  loglevel = debug
  actor {
    debug {
      receive = on
      lifecycle = on
    }
  }
}

detroitapi {
  foo = bar
}

http {
  interface="::0"
  port=7777
}
